title: Invest With Maries 
email: rmaries@gmail.com
description: >-
  AMFI Registered Mutual Fund Distributor (ARN-151461). 
  Global Engineering background with a passion for disciplined wealth creation through Indian Mutual Funds.
baseurl: "/InvestWithMaries" # the subpath of your site, e.g. /blog
url: "https://rmaries.github.io" # the base hostname & protocol for your site
github_username: rmaries

# Build settings
theme: jekyll-theme-minimal # Using a basic theme as a starting point, we'll customize
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-paginate

# Pagination for blog
paginate: 5
paginate_path: "/blog/page:num/"

# Permalinks
permalink: /:categories/:title/
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
      permalink: /blog/:title/
